# 标准管报生成能力 · 设计文档

> 版本 v0.1 · 2026-05-21
> 范围:报表分析(通用 AI 助手)下的「报表生成」能力。
> 关联实现:配置向导 UI 占位已落地(`bi_agent/web/static/` index.html / styles.css / app.js,缓存版本 v=54)。本文档描述完整设计,后端执行通道为下一阶段接入项。

---

## 1. 背景与目标

### 1.1 用户需求

标准管报生成:经营规则(相对复杂)的结构化、配置化;实现**从规则变化到标准管理报告变化的灵活、快速生成**。

### 1.2 设计目标

把一张管报拆成三件**可独立演进**的事:

```
管理报告 = 业务数据(明细)  ×  经营规则(口径/算法)  ×  报表模板(版式/结构)
```

今天三者耦合在 Excel 公式或代码里,改一条口径就要重做整张表。本能力把**经营规则**和**报表模板**抽成一等公民的、可配置、可版本化的元数据——规则一改,报表自动跟着变。

### 1.3 不在本期范围

- 规则中心的完整管理后台(本期仅定义数据模型,向导内消费已发布规则)。
- 真实的 Excel/PDF 渲染引擎(本期定义输出契约)。
- 多租户与企业级权限体系(本期权限切片为单层口径过滤)。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────┐
│  ⑤ 生成与呈现层   触发/调度 · 版本归档 · 在线表/Excel/PDF │
├─────────────────────────────────────────────────────┤
│  ④ 报表模板层     行列结构 · 单元格绑定 · 钻取 · 版式      │
├─────────────────────────────────────────────────────┤
│  ③ 指标语义层     原子指标 + 派生指标(口径唯一)          │ ← 复用现有「财务本体」
├─────────────────────────────────────────────────────┤
│  ② 规则层 ★       口径/分摊/调整/映射规则 + 版本 + 生效日期 │ ← 新建,核心
├─────────────────────────────────────────────────────┤
│  ① 数据层         金蝶 ERP 明细 · T_CFO_* · 原子事实       │
└─────────────────────────────────────────────────────┘
          AI Agent 横向贯穿 ②③④⑤:配规则 / 解读影响 / 写评述
```

关键:项目**已有「财务本体(ontology)」做指标语义层 ③**——指标定义已在本体里。本能力的增量是在本体之上加一个**规则层 ②**,让规则去参数化「指标怎么算」,以及一个**报表模板层 ④**。

---

## 3. 用户交互:报表生成向导

### 3.1 入口

`报表分析` 页面(报表模式)底部 composer 工具栏新增「⊞ 报表生成」按钮(仅报表模式可见)。点击弹出 7 步配置向导。

### 3.2 七步配置 → 任务书

向导的唯一产出物是一份结构化的**报表生成任务书(ReportGenTask)**——这是 agent 的输入契约。

| 步骤 | 选什么 | 写入任务书 | 依赖 |
|---|---|---|---|
| ① 业务数据底座 | 数据源 + 期间 + 对比基期 | `data_foundation` | — |
| ② 指标体系 | 指标集 + 口径来源 | `metric_system` | ① |
| ③ 经营规则配置 | 规则方案 + 规则明细启用项 | `business_rules` | ② |
| ④ 报表模板和输出 | 模板 + 版式 + 输出格式 | `template_output` | ②③ |
| ⑤ 权限切片 | 组织范围 + 业务线 + 可见口径 | `permission_slice` | — |
| ⑥ 可选图表库 | 配图类型 | `chart_library` | ④ |
| ⑦ 报表自动生成 | review 全量配置 → 最终确认 | 触发 agent | 全部 |

设计为分步向导(而非单表单)的原因:各步有依赖,后一步要被前一步过滤;每步带合理默认值,用户可快速点过。

### 3.3 向导关键能力(后续迭代)

- **存为方案**:任务书可保存为命名方案,下月换期间一键重跑——这是「快速生成」的关键。当前 UI 已有按钮占位。
- **影响预览**:③ 步切换规则前,提示受影响的指标/报表(当前为静态文案占位)。
- **沙箱 What-if**:发布前并排显示「改前 vs 改后」报表差异(后续迭代)。

---

## 4. 核心数据契约:报表生成任务书(ReportGenTask)

向导 ⑦ 确认后装配的 JSON,是前端与 agent 之间的**唯一契约**。

```jsonc
{
  "task_type": "standard_mgmt_report",
  "generated_at": "2026-05-21T08:00:00.000Z",

  "data_foundation": {
    "period": "2026年5月(当前期)",
    "sources": ["金蝶 ERP 明细库", "T_CFO_* 经营主题表", "财务本体语义层"],
    "compare_base": "上期环比(2026年4月)"
  },

  "metric_system": {
    "metric_sets": ["损益类 · 收入/成本/毛利/三费/净利",
                    "应收类 · 余额/账龄/逾期/回款率"],
    "caliber_source": "财务本体 · 已发布指标定义"
  },

  "business_rules": {
    "rule_plan": "部门损益 · 标准口径 v3",
    "rule_items": ["口径 · 收入不含税、剔除关联交易",
                   "分摊 · 总部费用按收入分摊至大区",
                   "调整 · 管报调整分录 3 条",
                   "映射 · 科目→管报行项目 已绑定"]
  },

  "template_output": {
    "template": "部门利润管报",
    "layout": "标准版",
    "output_formats": ["在线交互报表", "Excel"]
  },

  "permission_slice": {
    "org_scope": "全集团",
    "biz_lines": ["全部业务线"],
    "visibility": "管理层全口径"
  },

  "chart_library": ["趋势线图 · 指标时间序列",
                    "维度对比柱状图",
                    "占比饼图 · 结构分布"],

  "agent": { "runner": "open-claude", "grants": ["ontology", "database"] }
}
```

> **P0 现状**:向导 ⑦ 确认后,前端把上述配置组装成一段**自然语言报表生成请求**派发给数据分析 Agent(bi-analyst,具备本体+数据库权限)。Agent 逐步取数(过程展示在对话中)、用 `TableGenerate`/`ChartGenerate` 把报表输出到看板,看板末尾的「导出本轮报告」按钮负责导出。
>
> **P3 目标**:把自然语言请求升级为上述结构化 `ReportGenTask` JSON,并将值升级为**结构化引用**(规则用 `rule_id@version`、指标用 `metric_code`、模板用 `template_id`),由确定性执行计划编译,便于 agent 解析与复算。见 §6.1。

---

## 5. 规则中心:经营规则的结构化

「经营规则结构化」是用户需求里真正的难点。规则不是单一公式,而是一组异构规则的叠加。

### 5.1 规则分类

| 规则类型 | 例子 | 变化频率 |
|---|---|---|
| 口径规则(范围/过滤) | 收入是否含税、是否剔除关联交易 | 高 |
| 计算规则(公式) | 毛利率 =(收入−成本)/收入 | 中 |
| 分摊规则 | 总部费用按收入/人头/订单数分摊到大区 | 高 |
| 调整规则 | 管报口径手工调整(管报 vs 财报差异) | 高 |
| 抵消/合并规则 | 内部交易抵消、合并范围 | 中 |
| 映射规则 | 科目→管报行项目、组织→大区、产品→业务线 | 中 |
| 期间/汇率规则 | 期间归属、本位币折算 | 低 |

### 5.2 规则元数据模型(规则中心库表)

每条规则是一个可查询、可版本化、可做影响分析的对象:

```jsonc
// 表 T_RULE_Definition — 规则定义
{
  "rule_id":      "RUL-ALLOC-0007",
  "rule_name":    "总部费用分摊至大区",
  "rule_type":    "allocation",          // 口径/计算/分摊/调整/抵消/映射
  "scope": {                              // 适用范围:谁用这条规则
    "org":        ["全集团"],
    "biz_line":   ["*"],
    "report":     ["部门利润管报"]
  },
  "effective_from": "2026-01-01",          // 生效区间:什么时候起算
  "effective_to":   null,
  "priority":      100,                    // 多规则冲突时的优先级
  "expression": {                          // 表达体:按 rule_type 选形态
    "method":    "by_revenue",             // 分摊基准
    "pool":      "总部费用",
    "targets":   "大区"
  },
  "depends_on":   ["M_REVENUE", "M_HQ_EXPENSE"],   // 依赖的原子指标
  "output_to":   "M_REGION_GROSS_PROFIT",          // 绑定到哪个派生指标
  "version":     3,
  "status":      "published",              // 草稿/审批中/已发布/已归档
  "created_by":  "财务BP-华东",
  "approved_by": "财务总监",
  "updated_at":  "2026-04-28T10:00:00Z"
}
```

### 5.3 规则的表达形态(分类配置,不要万能编辑器)

- 口径规则 → 配置化表单(勾选项 + 多选范围)
- 分摊规则 → 向导式(分摊对象 + 基准下拉 + 比例表)
- 映射规则 → 映射表(支持批量导入/继承)
- 计算规则 → 受限公式编辑器(只能引用已登记指标,带语法+勾稽校验)
- 调整规则 → 调整分录表(带说明、附件、审批)

### 5.4 从「规则变化」到「报表变化」的机制

1. **版本 + 生效日期**:改规则 = 新建版本,不覆盖。历史期间用历史规则复算,结果可复现。
2. **影响分析**:改规则前反查依赖图(`depends_on` / `output_to`)——告知影响哪些指标、哪些报表。
3. **增量重算**:只重算受影响的指标分支。
4. **沙箱 What-if**:规则变更先在沙箱跑,并排显示改前/改后差异。
5. **审批 + 一键发布/回滚**。

---

## 6. Agent 执行协议(open-claude agent loop)

向导 ⑦ 确认 → 任务书提交给 open-claude agent(具备 `OntologyQuery` / `MetricLookup` / `RelationLookup` / `SQLRun` 等本体与数据库权限,与 `report-analyst` 同源)。

### 6.1 执行流水线

```
任务书(ReportGenTask)
   │
   ▼
[1] 编译确定性执行计划
    · 解析 metric_sets → 本体查 metric_code 与口径定义
    · 解析 rule_plan → 规则中心拉取「该期间生效」的规则版本(按 effective_from + priority)
    · 构建指标 DAG(原子指标 → 派生指标)
   │
   ▼
[2] SQLRun 逐项取数(只读 SELECT)
    · 每个原子指标编译成固定 SQL,在 data_foundation.sources 范围内执行
   │
   ▼
[3] 派生指标计算
    · 按规则执行分摊 / 调整 / 抵消 / 映射
   │
   ▼
[4] 勾稽校验(见 §7)
    · 报表内勾稽 + 跨表勾稽;不平则定位差异并标注
   │
   ▼
[检查点] 展示勾稽结果 → 用户确认
   │
   ▼
[5] 装配模板 + 生成图表 + 渲染输出
    · TableGenerate / ChartGenerate 渲染;按 output_formats 导出
    · LLM 撰写管理层评述(narrative)
   │
   ▼
版本归档:任务书 + 规则版本 + 数据快照(可复现)
```

### 6.2 确定性边界(最关键的设计约束)

标准管报必须**可复现、可审计**。因此严格划清 LLM 与确定性引擎的边界:

- **数字**来自配置编译出的**确定性执行计划**(指标 DAG + 规则解析 + 固定 SQL),**不是** LLM 即兴判断。同一任务书两次运行必须得到相同结果。
- **LLM 只做三件事**:① 把任务书翻译成执行计划;② 取数后查异常、定位差异;③ 生成管理评述。
- 一句话:agent 是「编译器 + 质检员 + 撰稿人」,不是「计算器」。

### 6.3 异步与检查点

- 长任务异步执行,前端推送进度状态;P0 阶段进度即 Agent 在对话中流式输出的取数步骤。
- 渲染输出前插入**人工检查点**:展示勾稽校验结果,用户确认后再产出最终报表。

---

## 7. 勾稽校验

| 校验类型 | 例子 |
|---|---|
| 报表内勾稽 | 各部门利润之和 = 集团利润;明细行之和 = 小计行 |
| 跨表勾稽 | 损益表净利 = 现金流表起算项;应收余额 = 期初+本期-回款 |
| 链路守恒 | 拆分/分摊后各子项之和 = 拆分前总额(参见 `backfill_deliver_stages.py` 中订-发链路勾稽守恒的做法) |
| 口径一致性 | 同一指标在不同报表的口径定义一致(由本体保证唯一口径) |

差异 ≥ 阈值时,agent 不自动改数,而是**定位并在检查点提示**用户。

---

## 8. 与现有系统的集成点

| 现有资产 | 集成方式 |
|---|---|
| 财务本体(ontology) | 指标语义层 ③ 直接复用;新增「规则」实体并与指标关联 |
| `T_CFO_*` / 金蝶明细表 | 数据层 ① 的原子事实来源 |
| `report-analyst` agent | 从「读上传报表」扩展为「读 + 配 + 生成」;复用其本体+DB 权限与 agent loop |
| `TableGenerate` / `ChartGenerate` | 报表模板层 ④ 的渲染后端 |
| `SQLRun`(只读) | 执行流水线 [2] 取数 |
| 配置向导(本次新增) | 任务书装配入口 |

---

## 9. 落地分期

| 阶段 | 交付 | 状态 |
|---|---|---|
| **P0 配置向导 + 报表生成 Agent** | 7 步向导;确认后在「报表分析」页起一个 report-generator 会话,Agent 系统性取数并生成结构化报表入看板,支持导出 HTML / Word | ✅ 已落地 |
| **P1 模板配置化** | 选 1 张高频管报(部门利润管报),版式做成可配模板 | 待开发 |
| **P2 规则抽取** | 规则中心数据模型落库;口径、分摊 2 类规则配置化 | 待开发 |
| **P3 结构化执行通道** | 自然语言请求 → 结构化 `ReportGenTask` → 编译执行计划 → 取数 → 勾稽 → 渲染 | 待开发 |
| **P4 版本与沙箱** | 规则版本/生效日期、影响分析、改前改后预览、审批发布 | 待开发 |
| **P5 AI 增强** | 自然语言配规则、自动管理评述、勾稽冲突校验 | 待开发 |

建议 P1+P2 先选一张报表打通端到端垂直切片,验证规则模型与任务书契约,再横向铺开。

---

## 附录 A · 已落地代码改动(P0)

| 文件 | 改动 |
|---|---|
| `bi_agent/web/static/index.html` | 「⊞ 报表生成」按钮(composer 工具栏,报表模式可见);7 步向导 modal `#reportgen-overlay` |
| `bi_agent/web/static/styles.css` | `.rg-*` 向导样式;`.composer-reportgen` 加入报表模式显隐规则 |
| `bi_agent/web/static/app.js` | 向导逻辑:`rgGoto` 步骤导航、`rgCollectPlan` 表单采集、`rgRenderReview` 复核、`rgBuildPrompt` 组装请求、`rgRunGeneration` 起会话并派发;`exportTurnReportWord` Word 导出 |
| `bi_agent/web/app.py` | `POST /api/report/generate` —— 起 report-generator 报表生成会话(本体+DB 工具,无需上传报表) |
| `.claude/agents/report-generator.md` | 报表生成 Agent —— 系统性检索数据、按报表模板产出多章节结构化报表 |

**P0 行为**:向导 ⑦ 确认 → `rgBuildPrompt` 把 7 步配置组装成报表生成请求 → `POST /api/report/generate` 在「报表分析」页起一个 report-generator 会话 → `sendMessage` 派发任务。Agent 系统性取数(步骤在对话流式展示),`TableGenerate`/`ChartGenerate` 把多章节报表输出到看板;看板末尾「📤 导出 HTML / 📄 导出 Word」由 `appendTurnExportButton` 提供。导出 Word 时先调用 `POST /api/report/compose` —— 一次大模型调用把看板内容整合成正式报表文档结构(标题 + 执行摘要 + 带章节简介的目录),前端再交织真实表格/图表组装为 `.doc`(大模型调用失败则回退为原始内容导出)。结构化执行通道(确定性编译 + 勾稽)见 P3。
